import { ProductLineTextPipe } from './product-line-text.pipe';

describe('ProductLineTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductLineTextPipe();
    expect(pipe).toBeTruthy();
  });
});
